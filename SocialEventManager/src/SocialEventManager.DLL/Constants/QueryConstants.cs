namespace SocialEventManager.DAL.Constants
{
    public static class QueryConstants
    {
        public const string SelectRowCount = "SELECT @@ROWCOUNT;";
        public const string SelectIdentity = "SELECT @@IDENTITY;";
    }
}
